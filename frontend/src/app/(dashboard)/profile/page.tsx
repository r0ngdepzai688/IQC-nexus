"use client";
import { SuccessState } from "@/components/portal/states";
import { useAuth } from "@/lib/contexts/AuthContext";
import { useState } from "react";
export default function ProfilePage() { const { user } = useAuth(); const [saved, setSaved] = useState(false); return <div className="page-stack"><section className="page-heading"><div><p className="eyebrow">ACCOUNT</p><h2>User Profile</h2><p>Review your server-provided identity and local display preferences.</p></div></section><section className="panel form-panel"><h3>{user?.fullName}</h3><dl className="definition-list"><div><dt>Username</dt><dd>{user?.username}</dd></div><div><dt>Role</dt><dd>{user?.systemRole}</dd></div><div><dt>Position</dt><dd>{user?.position || "—"}</dd></div><div><dt>Organization</dt><dd>{user?.organization || "—"}</dd></div></dl><label>Display language<select defaultValue="English"><option>English</option><option>Vietnamese</option></select></label>{saved && <SuccessState message="Preference saved for this session." />}<button className="primary-action" onClick={() => setSaved(true)}>Save preference</button><p className="muted">Roles and permissions are managed by administrators and enforced by the backend.</p></section></div>; }